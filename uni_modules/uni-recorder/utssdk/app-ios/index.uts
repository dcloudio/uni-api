import { GetRecorderManager, RecorderManager } from "../interface.uts"

export const getRecorderManager : GetRecorderManager = function () : RecorderManager {
	let recorderManager = new UniAudioRecorderManager.shared
	return recorderManager
}